> ※ 안내: 초급자 강의에는 **초급·중급** 문제를, 경력자 강의에는 **중급·고급** 문제를 활용하세요.

## 🟢 초급 문제

### Q1. Docker에서 **볼륨(Volume)**의 주요 기능은 무엇인가요?
1) 데이터의 임시 저장
2) 데이터의 영구 저장
3) 네트워크 트래픽 관리
4) CPU 사용량 모니터링

<details><summary>정답 보기</summary>

정답: 2번 — 볼륨은 Docker 컨테이너에서 생성된 데이터를 영구적으로 저장할 수 있도록 해줍니다.

</details>

### Q2. Docker에서 **컨테이너(Container)**의 주요 특징은 무엇인가요?
1) 데이터가 영구적으로 저장된다.
2) 다른 컨테이너와 독립적인 파일 시스템을 가진다.
3) 호스트 시스템과 동일한 파일 시스템을 공유한다.
4) 항상 실행 중이다.

<details><summary>정답 보기</summary>

정답: 2번 — 컨테이너는 다른 컨테이너와 독립적인 파일 시스템을 가지며, 이를 통해 각 컨테이너는 서로의 영향을 받지 않습니다.

</details>

## 🟡 중급 문제

### Q3. Docker에서 **볼륨을 생성**하기 위해 사용하는 CLI 명령어는 무엇인가요?
1) `docker create volume`
2) `docker volume create`
3) `docker create`
4) `docker volume new`

<details><summary>정답 보기</summary>

정답: 2번 — `docker volume create` 명령어를 사용하여 새로운 볼륨을 생성합니다.

</details>

### Q4. Docker Compose에서 **웹 서비스**와 **DB 서비스**를 구성할 때, 데이터 영속성을 보장하기 위해 어떤 설정을 사용해야 하나요?
1) 웹 서비스와 DB 서비스를 동일한 컨테이너에 배치한다.
2) DB 서비스에 볼륨을 마운트하여 데이터를 저장한다.
3) DB 서비스의 데이터를 메모리에 저장한다.
4) 웹 서비스에 DB 정보를 직접 포함시킨다.

<details><summary>정답 보기</summary>

정답: 2번 — DB 서비스에 볼륨을 마운트하여 데이터를 저장하면, 컨테이너가 종료되더라도 데이터가 유지됩니다.

</details>

### Q5. Docker에서 **데이터 백업**을 자동화하기 위한 가장 일반적인 방법은 무엇인가요?
1) 수동으로 컨테이너를 클라우드에 업로드한다.
2) 볼륨을 다른 서버로 복사한다.
3) Docker CLI를 사용하여 스크립트를 작성한다.
4) 컨테이너를 정기적으로 삭제한다.

<details><summary>정답 보기</summary>

정답: 3번 — Docker CLI를 사용하여 백업 스크립트를 자동화함으로써 데이터 백업을 쉽게 수행할 수 있습니다.

</details>

### Q6. Docker의 **볼륨**과 **바인드 마운트**의 차이점은 무엇인가요?
1) 볼륨은 호스트 파일 시스템에 의존하지 않는다.
2) 바인드 마운트는 영구적이지 않다.
3) 볼륨은 여러 컨테이너에서 공유할 수 없다.
4) 바인드 마운트는 Docker에 의해 관리된다.

<details><summary>정답 보기</summary>

정답: 1번 — 볼륨은 Docker가 관리하며, 호스트 파일 시스템에 독립적입니다.

</details>

## 🔴 고급 문제

### Q7. Docker 볼륨의 **라이프사이클**은 컨테이너와 어떻게 다른가요?
1) 볼륨은 항상 실행 중이다.
2) 볼륨은 컨테이너와 관계없이 독립적으로 존재한다.
3) 볼륨은 컨테이너가 종료될 때 삭제된다.
4) 볼륨은 항상 데이터가 손실되지 않는다.

<details><summary>정답 보기</summary>

정답: 2번 — 볼륨은 특정 컨테이너의 라이프사이클에 의존하지 않고 독립적으로 존재합니다.

</details>

### Q8. Docker 환경에서 **운영 자동화**를 구현할 때 고려해야 할 **트레이드오프**는 무엇인가요?
1) 성능 vs 안전성
2) 복잡성 vs 유연성
3) 비용 vs 품질
4) 스케일링 vs 데이터 무결성

<details><summary>정답 보기</summary>

정답: 2번 — 운영 자동화는 시스템의 복잡성을 증가시킬 수 있지만, 더 많은 유연성을 제공할 수 있습니다.

</details>