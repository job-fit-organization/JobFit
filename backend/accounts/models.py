from django.db import models
from django.contrib.auth.models import AbstractUser

# Create your models here.
# For this project, we can use the default User model or extend it.
# If using the default User model, we'll store the Kakao ID in the username field.
